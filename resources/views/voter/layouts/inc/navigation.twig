
     <!-- ==============================================
     Navigation Section
     =============================================== -->  
     <header class="tr-header">
      <nav class="navbar navbar-default">
       <div class="container">
      <div class="navbar-header">
     <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
     </button>
     <a class="navbar-brand" href="{{ path_for('home') }}">Voting</a>
    </div><!-- /.navbar-header -->
    <div class="navbar-left">
     <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">
       <li class=""><a href="{{ path_for('home') }}">Home</a></li>
       <li class=""><a href="">How it works</a></li>
      </ul>
     </div>
    </div><!-- /.navbar-left -->
    <div class="navbar-right">  

     <ul class="nav navbar-nav">
              <li class="dropdown user user-menu">
                <!-- Menu Toggle Button -->
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                
                  <!-- hidden-xs hides the username on small devices so only the image appears. -->
                  <span class="hidden-xs">
                    Languages
                  </span>
                </a>
                <ul class="dropdown-menu">

                  {% for lang in languages %}
                   <li class="m_2"><a onclick="changelanguage('. $rnav->languageid .')" style="cursor: pointer;">{{ lang.language }}</a></li>
                  {%endfor %}

            </ul>
              </li>
           </ul>

  {% if user.isLoggedIn %} 

    {% if user.usertype == 1 %} 

     <ul class="nav navbar-nav">
     <li class="dropdown mega-avatar">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="true">
       <span class="avatar w-32"><img src="{{ base_url() }}/admin/users/uploads/{{ user.imagelocation }}" class="img-resonsive img-circle" width="25" height="25" alt="..."></span>
       <!-- hidden-xs hides the username on small devices so only the image appears. -->
       <span>
            {{ user.name }}
       </span>
      </a>
      <div class="dropdown-menu w dropdown-menu-scale pull-right">
       <a class="dropdown-item" href="{{ path_for('voter.dashboard') }}"><span>Dashboard</span></a> 
       <a class="dropdown-item" href=""><span>Profile</span></a>
       <a class="dropdown-item" href="{{ path_for('logout') }}">Logout</a>
      </div>
     </li><!-- /navbar-item -->
     </ul>

     {% elseif user.usertype == 2 %} 

     <ul class="nav navbar-nav">
     <li class="dropdown mega-avatar">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="true">
       <span class="avatar w-32"><img src="{{ base_url() }}/admin/nominees/{{ user.imagelocation }}" class="img-resonsive img-circle" width="25" height="25" alt="..."></span>
       <!-- hidden-xs hides the username on small devices so only the image appears. -->
       <span>
            {{ user.name }}
       </span>
      </a>
      <div class="dropdown-menu w dropdown-menu-scale pull-right">
       <a class="dropdown-item" href=""><span>Dashboard</span></a> 
       <a class="dropdown-item" href=""><span>Profile</span></a>
       <a class="dropdown-item" href="{{ path_for('logout') }}">Logout</a>
      </div>
     </li><!-- /navbar-item -->
     </ul>

    {% endif %}   

    {% else %} 
       
     <ul class="nav navbar-nav">
      
      <li><i class="fa fa-user"></i></li>
      <li class=""><a href="{{ path_for('login') }}">Login</a></li>
     </ul><!-- /.sign-in -->   
     <a href="<?php echo $web->url; ?>register" class="kafe-btn kafe-btn-mint-small">
      Register
     </a>      

  {% endif %}          
    </div><!-- /.nav-right -->
       </div><!-- /.container -->
      </nav><!-- /.navbar -->
     </header><!-- Page Header -->
